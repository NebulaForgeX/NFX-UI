# PixelBlastBackground (moved)

**This document has moved.** `PixelBlastBackground` is **not** exported from `nfx-ui/animations`. Use the separate subpath `nfx-ui/pixel-blast`.

See: [pixel-blast/README.md](../pixel-blast/README.md)

---

# PixelBlastBackground（已迁移）

**本文档已迁移。** `PixelBlastBackground` **不在** `nfx-ui/animations` barrel 中，请使用独立子路径 `nfx-ui/pixel-blast`。

请参阅：[pixel-blast/README.md](../pixel-blast/README.md)
