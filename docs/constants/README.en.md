# Constants Module Documentation

Usage guide for shared constants and React Query key factories.

---

## Entry

Exported from `nfx-ui/constants` (external). In repo: `@/constants`.

```ts
import { CACHE_ITEM, CACHE_LIST, createItemKey, createKey, createListKey, createQueryKeys, defineEnum, enumPickMap } from "nfx-ui/constants";
```

---

See the main [README.md](./README.md) for full sections: Caches, createKey, createListKey, createItemKey, createQueryKeys, defineEnum, enumPickMap, and Prefix Order.
